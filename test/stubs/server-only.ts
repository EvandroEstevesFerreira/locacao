// Stub de `server-only` para o Vitest — ver o comentário em vitest.config.ts.
// O pacote real não exporta nada: ele só existe para o bundler do Next
// rejeitar o import a partir de um Client Component.
export {};
